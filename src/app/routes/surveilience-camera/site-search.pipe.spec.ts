import { SiteSearchPipe } from './site-search.pipe';

describe('SiteSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SiteSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
